name: "Verify utils build"
on:
  pull_request:
    paths:
      - "packages/utils/**.ts"
      - "packages/utils/**.json"
      - "yarn.lock"
  push:
    branches:
      - master

jobs:
  verify:
    uses: ./.github/workflows/verify.yaml
    with:
      workingDirectory: ./packages/utils
